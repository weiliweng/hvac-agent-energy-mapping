# Contributing

Contributions should preserve the project's evidence-first design:

- never add real client or building data;
- add a test for every mapping rule or failure mode;
- do not allow a model to invent equipment identifiers;
- keep abstention and human review as first-class outcomes;
- document changes to metric definitions.

Run `make verify` before opening a pull request.
